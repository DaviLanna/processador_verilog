// O módulo TOPO que junta tudo
module top_processor (
    input wire clk,
    input wire reset
);
    // TODO: Instanciar e conectar todos os módulos (PC, memórias, ULA, registradores, etc.)
endmodule
